{
  "name": "redirect",
  "version": "1.0.0",
  "description": "simple service to redirect user base on query",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mHealth Technologies Jsc",
  "license": "UNLICENSED",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "4.19.2"
  }
}
